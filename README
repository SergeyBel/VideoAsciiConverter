VideoAsciiConverter is a project to convert video to ascii video.

Usage: VideoConverter -i <intput file name> -o <output file name> -s <font scale> -c (optional)

-i <input file name> - name of video file to convert
-o <output file name> - name of result video file
-s <font scale> - font scale(double), set scale of ascii symbols
-c - if set then result video will be colored
